repos:
  - repo: https://github.com/charliermarsh/ruff-pre-commit
    # Pinned (was floating `stable`) so hook results match CI and
    # requirements.txt; bump all three together.
    rev: v0.16.2
    hooks:
      - id: ruff
        args: ["--fix"]
      - id: ruff-format